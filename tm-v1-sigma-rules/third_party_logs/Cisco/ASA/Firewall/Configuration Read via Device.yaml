title: Configuration Read via Device
description: You have entered the read command to read your configuration from a device (either floppy disk, flash memory, TFTP, the failover standby unit, or the console terminal). The IP address indicates whether the login was made at the console port or with a Telnet connection.
tags:
- mitre.t1016
logsource:
  definition: THIRDPARTY_THIRDPARTY
  category: THIRD_PARTY_LOG
  product: Cisco
detection:
  mappingtable:
    vendorLogId:
    - '111002'
  condition: mappingtable
level: info
taxonomy: tm-v1
