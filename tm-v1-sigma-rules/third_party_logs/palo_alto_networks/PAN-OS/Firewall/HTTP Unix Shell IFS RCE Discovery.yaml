title: HTTP Unix Shell IFS RCE Discovery
description: Palo Alto Networks detection for HTTP Unix Shell IFS Remote Code Execution Detection. This signature detects suspicious HTTP requests that use internal field separators like $IFS or ${IFS} which are often used in command injection to replace white space.
tags: []
logsource:
  definition: THIRDPARTY_THIRDPARTY
  category: THIRD_PARTY_LOG
  product: PALO ALTO NETWORKS
detection:
  subname:
    eventSubName: vulnerability
  rule:
    ruleId:
    - '56830'
    - '95197'
  condition: subname and rule
level: info
taxonomy: tm-v1
